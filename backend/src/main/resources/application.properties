spring.application.name=backend

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/loan_tracker}
spring.datasource.username=${DB_USER:loan_user}
spring.datasource.password=${DB_PASSWORD:Noeani15}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true